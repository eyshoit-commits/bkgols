[package]
name = "bkg-plugin-api"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "BKG plugin interface for modular sandbox runtime"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
async-trait = "0.1"
