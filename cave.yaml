namespace: default
plugin_directory: plugins/dist
environment:
  RUST_LOG: info
telemetry:
  enabled: true
  sampling_interval: 1.0
server:
  bind_address: 0.0.0.0
  port: 8080
  health_path: /healthz
  metrics_path: /metrics
plugins:
  bkg.logger:
    options:
      level: info
